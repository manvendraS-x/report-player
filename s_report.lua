webHookURL = "https://discordapp.com/api/webhooks/647856602753269760/2vCrF-wCfXY9ofX3M8vrFKmVWbq89T9N2GrFBAA8hkwCobDp2xPZnohh2qNqTdmHeRsU" -- The Webhook link to post too.
local DISCORD_NAME = "Report Bot" -- Changes the name of the bot (default : Report Bot)
local DISCORD_IMAGE = "https://newsfeed.org/wp-content/uploads/social-media-reports-Facebook-reports-Instagram-reports.jpg" -- Any Image URL works
local role = "576206644674035712" -- Must be a role id
enableReport = true -- Enable /report command

local role = "<@&"..role..">"

if enableReport then
    RegisterCommand("report", function(source, args, rawCommand)
        local user = tonumber(args[1])
        local msg = table.concat(args, " ", 2)
        local username = GetPlayerName(user)
		local reporter = GetPlayerName(source)
		if username == nil then
			print("Please assure that the ID is correct (invalid or no ID detected)")
		else
			color = 16711680
			print("The User".. username .. " Was Reported to Staff!")
            
            local ping = GetPlayerPing(user)
            local identifiers = GetPlayerIdentifiers(user)
            local data = json.encode(identifiers)

            local connect = {
                {
                    ["color"] = color,
                    ["title"] = "The User ["..user.."] ".. username .. " Was Reported For: ",
                    ["description"] = "Reason: **"..msg.."**\nPing: **"..ping.."**\nIdentifiers: **"..data.."**",
                    ["footer"] = {
                    ["text"] = "Report Generated By : "..reporter.. ".",
                    },
                }
            }
            PerformHttpRequest(webHookURL, function(err, text, headers) end, 'POST', json.encode({username = DISCORD_NAME, content = role , embeds = connect, avatar_url = DISCORD_IMAGE}), { ['Content-Type'] = 'application/json' })
		end
    end)

end
